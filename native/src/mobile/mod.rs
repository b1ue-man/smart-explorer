//! Android core facade (JSON commands, tasks and events behind the JNI bridge).
//!
//! Placeholder so the crate and the `android-bridge` workspace member compile;
//! the facade itself is filled in by the Android facade milestone.
