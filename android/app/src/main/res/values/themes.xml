<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose draws everything; the window theme only sets the pre-Compose background and
         the system bar defaults that enableEdgeToEdge() then takes over. -->
    <style name="Theme.SmartExplorer" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/window_background_light</item>
    </style>
</resources>
